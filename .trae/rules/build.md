1. 更改项目代码后需对代码进行格式化，统一代码风格
2. 更改项目代码后需重新构建项目并进行typecheck
3. 使用 `pnpm` 作为唯一的包管理器，不要使用 `npm` 或 `yarn`
4. 使用 `git` 进行版本控制，提交代码时需添加提交信息
5. 项目代码需符合 `ESLint` 规范
6. 自定义的 skills 需在 `domain/.trae/skills` 目录下，文件名需以 `domain-manager-` 开头
